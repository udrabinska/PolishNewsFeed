<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/standard_padding">

    <TextView
        android:id="@+id/title_view"
        style="@style/textStyle"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="@dimen/title_text"
        android:textStyle="bold"
        tools:text="EU demands action by Poland's government to protect rule of law" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/author_view"
            style="@style/textStyle"
            tools:text="Daniel Boffey in Brussels" />

        <TextView
            style="@style/textStyle"
            android:text="@string/divider" />

        <TextView
            android:id="@+id/date_view"
            style="@style/textStyle"
            tools:text="14.05.2018" />
    </LinearLayout>

    <ImageView
        android:id="@+id/image_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/smaller_padding"
        android:contentDescription="@string/image_description"
        android:scaleType="centerCrop"
        tools:background="@color/colorAccent" />

    <TextView
        android:id="@+id/trailer_view"
        style="@style/textStyle"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="@dimen/lead_text"
        tools:text="Poland may face sanctions if it cannot show it has retracted controversial legal changes" />
</LinearLayout>